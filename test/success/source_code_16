// Função add + locals na main — esperado: 8
num m = 10;
num n = 4;

func add(num a, num b) num {
  return a + b;
}

func main() num {
  num acc = 0;
  while m + 1 > n {
    m = m - n;
    acc = add(acc, m);
  }
  return acc;
}
