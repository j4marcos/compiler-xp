func fill(list arr, num v) num {
  arr.push(v);
  return arr.len;
}

func make() list {
  list t = [1, 2];
  t.push(3);
  return t;
}

func main() num {
  list a = make();
  list b = a;
  a.push(4);
  fill(b, 5);
  return b.len;
}
