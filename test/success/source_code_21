// if em função + locals — esperado: 10
func abs(num x) num {
  if x < 0 {
    return 0 - x;
  } else {
    return x;
  }
}

func main() num {
  num a = abs(0 - 7);
  num b = abs(3);
  return a + b;
}
